# =========================
# KAIRA hyprlock palette
# =========================

# Core text (warm ivory / sunlight)
$lock_text      = rgba(246, 231, 215, 1.0)   # ivory (#F6E7D7)
$lock_text_dim  = rgba(246, 231, 215, 0.70)  # softened
$lock_textalt   = rgba(ca2b4bff)   # lilac accent (#e693d9)

# Glass + input field
# Warm crimson glass aesthetic
$lock_glass     = rgba(100, 30, 60, 0.22)    # crimson glass (#C83A5C)
$lock_field     = rgba(22, 24, 34, 0.55)     # cool charcoal input field
$lock_imageback = rgba(12, 14, 20, 0.94)     # deep night backing for logo

# Accent + glow
$lock_accent    = rgba(224, 90, 120, 0.90)  # gold highlight (#fff9a5)
$lock_glow      = rgba(255, 249, 165, 1.0)   # rose glow (#E05A78)
$bar_fill       = rgba(255, 249, 165, 1.0)   # gold battery fill

# Optional lilac accent
# $lock_accent = rgba(230, 147, 217, 0.92)   # lilac (#e693d9)
# $bar_fill    = rgba(230, 147, 217, 1.0)

# =========================
# Backwards-compat mapping
# =========================
$color        = $lock_glass
$inner_color  = rgba(246, 231, 215, 0.10)
$outer_color  = $lock_accent
$font_color   = $lock_text

# Success / confirmation tint
$check_color  = rgba(224, 90, 120, 0.95)

# =========================
# Paths / font
# =========================
$lock_bg   = /home/kazuki/.config/themes/lunr/backgrounds/lunr-bg.png
$lock_logo = /home/kazuki/.config/themes/kaira/KairaLogo.png

$lock_font = Google Sans Display,Google Sans Display Medium
